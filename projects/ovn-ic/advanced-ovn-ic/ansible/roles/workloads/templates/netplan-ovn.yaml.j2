network:
  version: 2
  ethernets:
    eth0:
      dhcp4: false
      addresses: [{{ wl.ip }}/24]
      mtu: {{ overlay_mtu }}
      routes:
        - to: default
          via: {{ vpc_gw }}
      nameservers:
        addresses: [1.1.1.1]
